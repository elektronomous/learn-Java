1. How do most operating systems keep track of what type of data
   is stored in a file?

   = OS recognizes the file by look at the extension file.

2. What kind of program would be used to create a file containing text?

   = Text editor

3. Directories are called ___ in some operating system.

   = Folder

4. Describe the difference between launching a GUI program and launching
   a text-based program.

   = The GUI program needs you to click on an icon to run the program, instead
     of type the name of the program.