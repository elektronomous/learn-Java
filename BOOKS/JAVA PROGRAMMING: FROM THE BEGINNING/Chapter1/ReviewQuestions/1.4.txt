1. In programming, numbers that contain a decimal point are called ___ numbers.

    = Floating-point

2. What is the name of the code that java uses to represent characters?

    = Unicode

3. Most modern computers group bits into larger units called ___, which typically
   contain eight bits.

    = Byte

4. Locations that store data are called ___.

    = Variable