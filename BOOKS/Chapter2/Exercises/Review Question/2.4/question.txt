1. What is the name of = operator ?
=  Assignment operator

2. Suppose that i is a variable declared in a method. What happens if a statement
   tries to access the value of i(to print it, for example) before i has been
   initialized?
=  The compiler will generate an error that ask you to initialize the variable first
   then access it(print it);

3. In the declaration
        int i = 0;
   we say that 0 is an
=  Initializer.