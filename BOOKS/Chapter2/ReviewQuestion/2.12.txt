1. Attempting to convert a nonnumeric string into an integer causes:
   (a) a compile-time error (b) a run-time error, or (c) inccorrect behavior

   = (b)

2. In Java, run-time errors are called ___.

   = Exceptions

3. What are the two major capabilities of a debugger?
   
   = checking if the statements are being executed in proper order
     and variables in proper values.

4. What's the best way to debug without a debugger?

   = Use System.out.prinln() method to see the variable that's you're interested in.